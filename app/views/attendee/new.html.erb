<h1>Invite Attendees to your events</h1>

<%# <%= render 'form', attendee: @attendees > %>

<%# <%= link_to 'Back', users_path %> 

<%# <h1 class="page_title">Log In</h1> %>

<%= form_tag attendee_new_path, method: :post do %>
    <%= label_tag  "Username" %>
   <%= text_field_tag :username, params[:username] %>
    <%= submit_tag("Invite") %>
<% end %>
